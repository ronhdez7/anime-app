import { HomeIcon as OutlineHomeIcon } from "react-native-heroicons/outline";
import { HomeIcon as SolidHomeIcon } from "react-native-heroicons/solid";
import { createIcon } from "./IconFactory";

export const HomeOutlineIcon = createIcon(OutlineHomeIcon);
export const HomeFilledIcon = createIcon(SolidHomeIcon);
